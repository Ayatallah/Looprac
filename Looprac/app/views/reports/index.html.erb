<!DOCTYPE html>
<html>
<head>
	<title>Report a User</title>
	<script type="text/javascript">
		$(function(){

			$(".dropdown-menu li a").click(function() {

				$(".dropdown-toggle:first-child").text($(this).text());
				$(".dropdown-toggle:first-child").val($(this).text());				


			});

		});
	</script>
</head>
<body class="landing">
	<div class="container">
		<div class="jumbotron">
			<div class="row">
				<div class="col-sm-12">
					<h2>Reporting a user.</h2>
				</div>
			</div>
			<br>
			<div class="row">
				<div class="col-sm-12">
					<h3>First Select the User you want to report:</h3>
					<div class="btn-group" role="roup">
						<button class="btn btn-default disabled" type="button">Users</button>

						<div class="btn-group">
							<button id="drop-togg" class="btn btn-default dropdown-toggle" data-toggle="dropdown"> Select One</span></button> 
							<ul class="dropdown-menu" role="menu">
								<% @users.each do |user| %>
									<% if user.username != current_user.username %>
										<li> <a href="#"+<% user.username %>> <%= user.username %> </a></li>
									<% end %>
								<% end %>
							</ul>
						</div>
					</div>
					<br> <br>
					<%= form_for Report.new do |f| %>
						 <%= f.hidden_field :reporter, :input_html => { :value => "current_user.username" } %>
						 <%= f.hidden_field :reported, :input_html => { :value => ".dropdown-toggle:first-child.text()" } %>

						 <%= f.submit "Report User", class: "btn btn-primary", type: "submit" %>
					<% end %>

				</div>
					
			</div>	
		</div>
	</div>
</body>
</html>